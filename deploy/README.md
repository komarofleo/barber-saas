# 🚀 Деплой и миграции AutoService SaaS

## 📋 Обзор

Эта папка содержит скрипты и инструкции для деплоя и применения миграций на сервере и локально.

## 📁 Файлы

| Файл | Описание | Использование |
|------|----------|-------------|
| `apply_migrations_local.sh` | Применение миграций локально | Локальная разработка |
| `apply_migrations_remote.sh` | Применение миграций на сервере | Продуктивная среда |
| `APPLY_MIGRATIONS.md` | Подробная инструкция по миграциям | Документация |
| `README.md` | Этот файл | Обзор скриптов |

## 🚀 Быстрый старт

### Локально

```bash
# 1. Сделайте скрипт исполняемым
chmod +x deploy/apply_migrations_local.sh

# 2. Запустите скрипт
./deploy/apply_migrations_local.sh

# 3. Следуйте инструкциям в скрипте
```

### На сервере

```bash
# 1. Сделайте скрипт исполняемым
chmod +x deploy/apply_migrations_remote.sh

# 2. Обновите конфигурацию в скрипте
nano deploy/apply_migrations_remote.sh

# Измените:
# SERVER_USER="your_username"
# SERVER_HOST="your_server.com"
# SERVER_PATH="/path/to/avtoservis"

# 3. Запустите скрипт
./deploy/apply_migrations_remote.sh
```

## 📋 Требования

### Локально

- Docker 20.10+
- Docker Compose 2.0+
- Python 3.9+
- `.env` файл в корне проекта

### На сервере

- SSH доступ к серверу
- Docker и Docker Compose установлены
- Проект развернут на сервере
- `.env` файл настроен

## 🔧 Скрипты

### apply_migrations_local.sh

Автоматический скрипт для применения миграций локально:

**Функции:**
- ✅ Проверка наличия Docker и Docker Compose
- ✅ Проверка наличия `.env` файла
- ✅ Проверка статуса контейнеров
- ✅ 4 способа применения миграций:
  1. Python скрипты (рекомендуется)
  2. SQL скрипты (альтернатива)
  3. Alembic CLI
  4. Все способы по очереди
- ✅ Применение начальных данных (seed)
- ✅ Проверка результата
- ✅ Перезапуск web контейнера
- ✅ Цветной вывод для удобства

**Использование:**
```bash
./deploy/apply_migrations_local.sh
```

### apply_migrations_remote.sh

Автоматический скрипт для применения миграций на сервере через SSH:

**Функции:**
- ✅ Проверка SSH соединения
- ✅ Применение миграций через Python скрипты
- ✅ Заполнение начальных данных
- ✅ Проверка результата
- ✅ Вывод созданных таблиц и данных
- ✅ Перезапуск web контейнера (опционально)
- ✅ Цветной вывод для удобства

**Конфигурация:**
```bash
SERVER_USER="your_username"  # Логин на сервере
SERVER_HOST="your_server.com"  # IP или домен сервера
SERVER_PATH="/path/to/avtoservis"  # Путь к проекту
```

**Использование:**
```bash
# 1. Обновите конфигурацию
nano deploy/apply_migrations_remote.sh

# 2. Запустите скрипт
./deploy/apply_migrations_remote.sh
```

## 📖 Документация

- **[APPLY_MIGRATIONS.md](APPLY_MIGRATIONS.md)** - Подробная инструкция по всем способам применения миграций
- **[../MIGRATION_GUIDE.md](../MIGRATION_GUIDE.md)** - Полное руководство по миграциям
- **[../ENV_SETUP_GUIDE.md](../ENV_SETUP_GUIDE.md)** - Настройка переменных окружения
- **[../MIGRATION_SUMMARY.md](../MIGRATION_SUMMARY.md)** - Сводка по всем способам миграций

## 🔄 Способы применения миграций

### Способ 1: Python скрипты (рекомендуется)

```bash
docker exec autoservice_web python3 scripts/migrate.py
docker exec autoservice_web python3 scripts/seed.py
```

### Способ 2: SQL скрипты (альтернатива)

```bash
docker exec -i autoservice_postgres psql -U autoservice_user -d autoservice_db < sql/create_multi_tenant_tables.sql
docker exec -i autoservice_postgres psql -U autoservice_user -d autoservice_db < sql/seed_data.sql
```

### Способ 3: Alembic CLI

```bash
docker exec autoservice_web alembic upgrade head
```

## ✅ Проверка результата

### Проверка таблиц

```bash
docker exec autoservice_postgres psql -U autoservice_user -d autoservice_db -c '\dt public.*'
```

### Проверка начальных данных

```bash
# Тарифные планы
docker exec autoservice_postgres psql -U autoservice_user -d autoservice_db -c 'SELECT * FROM plans;'

# Супер-администраторы
docker exec autoservice_postgres psql -U autoservice_user -d autoservice_db -c 'SELECT * FROM super_admins;'
```

### Проверка через API

```bash
curl http://localhost:8000/api/health
curl http://localhost:8000/api/public/plans
```

## 🐛 Устранение проблем

### Проблема: "permission denied"

**Решение:** Сделайте скрипты исполняемыми

```bash
chmod +x deploy/apply_migrations_local.sh
chmod +x deploy/apply_migrations_remote.sh
```

### Проблема: "docker: command not found"

**Решение:** Установите Docker

```bash
# Для Ubuntu/Debian
sudo apt-get update
sudo apt-get install docker.io docker-compose

# Для CentOS/RHEL
sudo yum install docker docker-compose
```

### Проблема: "connection refused"

**Решение:** Проверьте статус контейнеров

```bash
docker ps
docker-compose up -d
```

### Проблема: "No module named 'app'"

**Решение:** Установите PYTHONPATH

```bash
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
```

## 📊 Данные для входа

После применения миграций:

### Супер-администратор

- **Username:** `admin`
- **Email:** `admin@autoservice.com`
- **Password:** `admin123`

⚠️ **Важно:** Обязательно измените пароль после первого входа!

### API Endpoints

- **Health:** `http://localhost:8000/api/health`
- **Plans:** `http://localhost:8000/api/public/plans`

### Frontend страницы

- **Админ-панель:** `http://localhost:3000`
- **Страница регистрации:** `http://localhost:3000/register`

## 🎯 После успешного применения

1. ✅ Перезапустите web контейнер
2. ✅ Проверьте работу API
3. ✅ Откройте Frontend в браузере
4. ✅ Войдите как супер-администратор
5. ✅ Измените пароль после первого входа
6. ✅ Протестируйте регистрацию нового клиента

## 📚 Полезные команды

```bash
# Логи контейнеров
docker logs -f autoservice_web
docker logs -f autoservice_postgres

# Статус контейнеров
docker ps

# Перезапуск контейнеров
docker-compose restart web

# Остановка контейнеров
docker-compose down

# Запуск контейнеров
docker-compose up -d

# Подключение к PostgreSQL
docker exec -it autoservice_postgres psql -U autoservice_user -d autoservice_db

# Проверка таблиц
\dt public.*
\dt tenant_001.*

# Выйдите из PostgreSQL
\q
```

## 🚨 Безопасность

- Не коммитьте `.env` файлы в Git
- Используйте сложные пароли (минимум 32 символа для секретных ключей)
- Измените пароль супер-администратора после первого входа
- Используйте SSH ключи для аутентификации
- Обновляйте зависимости регулярно

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи контейнеров
2. Проверьте `.env` файл
3. Проверьте документацию в `APPLY_MIGRATIONS.md`
4. Проверьте полную документацию в `../MIGRATION_GUIDE.md`

---

**Для локального применения миграций:**
```bash
./deploy/apply_migrations_local.sh
```

**Для применения миграций на сервере:**
```bash
./deploy/apply_migrations_remote.sh
```

**Для подробной информации:**
[APPLY_MIGRATIONS.md](APPLY_MIGRATIONS.md)

