# Инструкция по тестированию напоминаний

## Тест: Напоминание для Ивана Петрова на завтра в 10:00

### Вариант 1: Через веб-интерфейс (рекомендуется)

1. Откройте веб-интерфейс: http://localhost:3000
2. Войдите как администратор
3. Перейдите в раздел "Записи" (Bookings)
4. Нажмите "+ Новая запись"
5. Создайте запись:
   - Клиент: Иван Петров (если нет - создайте нового клиента)
   - Дата: Завтра
   - Время: 10:00
   - Услуга: Любая активная услуга
   - Статус: **confirmed** (подтверждена)
6. Сохраните запись

**Результат:** Напоминания будут автоматически запланированы:
- Напоминание за день: сегодня в 18:00 (если запись на завтра)
- Напоминание за 3 часа: завтра в 07:00 (за 3 часа до 10:00)

### Вариант 2: Через Telegram бота

1. Откройте Telegram бота
2. Войдите как администратор
3. Меню → Админ → Записи → Создать запись
4. Выберите клиента "Иван Петров" (или создайте нового)
5. Выберите дату: завтра
6. Выберите время: 10:00
7. Выберите услугу и мастера
8. Подтвердите создание

**Результат:** При подтверждении записи напоминания будут автоматически запланированы.

### Вариант 3: Проверка логов Celery

После создания и подтверждения записи проверьте логи:

```bash
docker-compose logs celery-worker | grep "Запланировано напоминание"
```

Должны увидеть сообщения:
- `📅 Запланировано напоминание за день: запись X, время отправки: ...`
- `⏰ Запланировано напоминание за 3 часа: запись X, время отправки: ...`

### Вариант 4: Прямой вызов функции отправки (для теста)

Если нужно протестировать отправку напоминания прямо сейчас (без ожидания):

1. Найдите `booking_id` записи в БД
2. Вызовите функцию напрямую через Python:

```python
import asyncio
from web.backend.app.tasks.notifications import send_single_reminder_day_before

# Замените company_id и booking_id на реальные значения
asyncio.run(send_single_reminder_day_before(company_id=1, booking_id=123))
```

### Проверка отправки

1. **Логи Celery Worker:**
   ```bash
   docker-compose logs -f celery-worker
   ```

2. **Логи Celery Beat:**
   ```bash
   docker-compose logs -f celery-beat
   ```

3. **Telegram бот:** Проверьте, что клиент получил сообщение с кнопками "✅ Подтверждаю" и "❌ Отказ"

### Ожидаемое поведение

- ✅ При подтверждении записи создаются 2 отложенные Celery задачи
- ✅ Напоминание за день отправляется в 18:00 за день до записи
- ✅ Напоминание за 3 часа отправляется за 3 часа до начала записи
- ✅ Каждое напоминание отправляется только один раз
- ✅ В сообщении есть кнопки "✅ Подтверждаю" и "❌ Отказ"

### Устранение проблем

Если напоминания не отправляются:

1. Проверьте, что `celery-worker` и `celery-beat` запущены:
   ```bash
   docker-compose ps
   ```

2. Проверьте логи на ошибки:
   ```bash
   docker-compose logs celery-worker | grep -i error
   ```

3. Проверьте, что у клиента есть `telegram_id` в таблице `users`

4. Проверьте, что у компании есть `telegram_bot_token` в таблице `public.companies`
