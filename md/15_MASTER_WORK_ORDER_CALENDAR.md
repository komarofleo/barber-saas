# 📅 Календарь лист‑нарядов для мастера (Telegram)

## Цель
Сделать удобный выбор даты лист‑наряда в Telegram для мастера:
- сначала показывать лист‑наряд на сегодня;
- затем дать календарь, где видны только даты с записями;
- при выборе даты показывать лист‑наряд на выбранный день.

## Пользовательский сценарий
1. Мастер нажимает **«📋 Лист‑наряд»**.
2. Бот показывает лист‑наряд **на сегодня**.
3. Ниже доступна кнопка **«📅 Календарь»**.
4. Мастер открывает календарь, видит **только занятые даты** (есть записи).
5. Нажимает дату → бот показывает лист‑наряд на выбранный день.

## Данные
Источник — таблица `bookings` в tenant‑схеме компании:
- фильтр: `master_id = <мастер>`,
- дата: `service_date`,
- статусы для отображения: `new`, `confirmed`.

## UI/UX
Календарь строится на основе `bot.utils.calendar.generate_calendar`:
- показываются только даты, где есть записи;
- по умолчанию отображается текущий месяц;
- навигация по месяцам доступна.

## Callback‑схема
Чтобы не конфликтовать с клиентским календарём:
- `master_calendar_date_YYYY_MM_DD`
- `master_calendar_month_YYYY_MM`

## Обработка ошибок
- если нет дат с записями → сообщение «Записей нет», календарь не показываем;
- если дата выбрана, но записей нет → показать «Записей нет на выбранную дату».
