# Role and Context
You are an expert developer. Follow these rules strictly to ensure code quality and consistency.

# Documentation & Knowledge (MCP)
- **Always use the Context7 MCP** to fetch the latest documentation for any libraries, APIs, or frameworks used in this project [2].
- Do not rely on your internal training data for version-specific syntax if Context7 can provide updated info [2].

# Task Management & Planning
- Before writing any code, check the `tasks.md` file for the current roadmap [3].
- Always update `tasks.md` after completing a sub-task [3].
- Follow a "Specification-First" approach: create a technical design document before major implementations.

# Code Style & Architecture
- Follow the established naming conventions (e.g., PascalCase for components, camelCase for variables).
- Ensure consistency between backend and frontend when modifying shared API endpoints [4].
- Maintain strict modularity and separation of concerns.

# Quality Control
- After implementing changes, suggest running **Bugbot** or **CodeRabbit** for an automated code review [5].
- Use **Sequential Thinking** for complex architectural decisions to avoid logic gaps.


# 🎯 Правила разработки AutoService SaaS

**Проект:** AutoService SaaS (Multi-Tenant)  
**Версия:** 2.0  
**Язык разработки:** Русский

---

## 📋 Содержание

1. [Язык и стиль кода](#язык-и-стиль-кода)
2. [Архитектура](#архитектура)
3. [Правила работы с модулями](#правила-работы-с-модулями)
4. [Мульти-тенантность](#мульти-тенантность)
5. [База данных](#база-данных)
6. [Backend](#backend)
7. [Frontend](#frontend)
8. [Telegram Bot](#telegram-bot)
9. [Celery Tasks](#celery-tasks)
10. [Безопасность](#безопасность)
11. [Git и коммиты](#git-и-коммиты)

---

## 🌐 Язык и стиль кода

### Язык
- ✅ **ВСЕ комментарии в коде на русском языке**
- ✅ **ВСЕ сообщения пользователю в логах на русском**
- ✅ **ВСЕ названия переменных и функций на русском** (если это улучшает читаемость)
- ✅ **ВСЕ документация на русском языке**

### Стиль кода

#### Python (Backend, Bot, Celery)
- Используй **Type hints**
- Форматируй код с помощью **black**
- Длина строки максимум **120 символов**
- Импорты должны быть отсортированы: стандартные, затем third-party
- Классы должны наследоваться от `object`
- Все async функции должны быть аннотированы
- Docstrings должны быть в тройных кавычках

#### TypeScript/JavaScript (Frontend)
- Используй **явные типизации**
- Используй **const** вместо **let** где возможно
- Интерфейсы должны быть четко типизированы
- Используй **arrow function** для однострочных функций

### Форматирование

- Используй **4 пробела** для отступов (PEP 8)
- Используй **двойные кавычки** для строк (f"имя файла")
- Пустые строки: `""`
- Документируй сложные выражения

### Docstrings

```python
# ✅ ПРАВИЛЬНО: Полная docstring с описанием
async def create_company(session: AsyncSession, name: str) -> Company:
    """
    Создать новую компанию.
    
    Args:
        session: Асинхронная сессия БД
        name: Название компании
    
    Returns:
        Объект созданной компании
    """
    company = Company(name=name, is_active=True)
    session.add(company)
    await session.commit()
    return company
```

---

## 🏗 Архитектура

### Мульти-тенантность

**КРИТИЧЕСКИ:** Все операции с БД должны учитывать `company_id` и использовать правильную схему!

```python
# ✅ ПРАВИЛЬНО: Использование tenant-сессии
async def get_bookings_for_company(company_id: int):
    """Получить записи для конкретной компании"""
    async for session in get_tenant_session(company_id):
        result = await session.execute(
            select(Booking).where(Booking.company_id == company_id)
        )
        return list(result.scalars().all())

# ❌ НЕПРАВИЛЬНО: Использование обычной сессии
async def get_bookings_for_company(company_id: int):
    result = await session.execute(
        select(Booking).where(Booking.company_id == company_id)
    )
    return list(result.scalars().all())
```

### Изоляция данных

**КРИТИЧЕСКИ:** Никогда не позволяйте доступ к данным одной компании из другой!

```python
# ✅ ПРАВИЛЬНО: Проверка изоляции
async def create_booking_for_company(company_id: int, booking_data: BookingCreate):
    """Создать запись для конкретной компании"""
    # Проверяем, что запись создается для правильной компании
    async for session in get_tenant_session(company_id):
        booking = Booking(**booking_data.dict())
        session.add(booking)
        await session.commit()
        return booking

# ❌ НЕПРАВИЛЬНО: Без проверки изоляции
async def create_booking(company_id: int, booking_data: BookingCreate):
    booking = Booking(**booking_data.dict())
    session.add(booking)
    await session.commit()
    return booking
```

### Доступ к публичным схемам

**КРИТИЧЕСКИ:** Только специфичные модули могут работать с `public` схемой!

```python
# ✅ ПРАВИЛЬНО: Разделение на tenant и public модули
# web/backend/app/api/public.py
async def register_company(request: CompanyRegistration, db: AsyncSession = Depends(get_db)):
    """Публичная регистрация новой компании"""
    # Создание компании, платежа, подписки
    pass

# web/backend/app/api/super_admin.py
async def get_all_companies(current_user: SuperAdmin = Depends(get_current_super_admin)):
    """Получить все компании (для супер-админа)"""
    async for session in get_session_with_schema("public"):
        result = await session.execute(
            select(Company).where(Company.is_active == True)
        )
        return list(result.scalars().all())

# ❌ НЕПРАВИЛЬНО: Смешивание схем без проверки
async def get_all_companies(current_user: SuperAdmin):
    result = await session.execute(
        select(Company)
    )
    return list(result.scalars().all())
```

---

## 🧩 Правила работы с модулями

### Backend модули

#### web/backend/app/api/

**Правила:**
- Все публичные API в `public.py`
- API для супер-админа в `super_admin.py`
- API для обычных компаний в соответствующих файлах
- Все API endpoints должны проверять подписку перед созданием записей

```python
# ✅ ПРАВИЛЬНО: Отдельный файл для публичных API
# web/backend/app/api/public.py

@router.post("/public/companies/register")
async def register_company(request: CompanyRegistration, db: AsyncSession = Depends(get_db)):
    """Публичная регистрация новой компании"""
    # Создание компании, платежа, подписки
    pass

# ✅ ПРАВИЛЬНО: API для супер-админа
# web/backend/app/api/super_admin.py

@router.get("/super-admin/dashboard/stats")
async def get_dashboard_stats(current_user: SuperAdmin = Depends(get_current_super_admin)):
    """Статистика для супер-админа"""
    pass

# ❌ НЕПРАВИЛЬНО: Смешивание публичных и приватных API
# web/backend/app/api/companies.py

@router.get("/companies")
async def get_companies():
    """Получить все компании (для супер-админа)"""
    pass
```

#### web/backend/app/models/

**Правила:**
- Модели для public схемы: `Company`, `Plan`, `Subscription`, `Payment`, `SuperAdmin`
- Модели для tenant схем: существующие модели
- Все модели должны иметь явные типы

```python
# ✅ ПРАВИЛЬНО: Модели в отдельных файлах
# web/backend/app/models/company.py
class Company(Base):
    """Модель компании в public схеме"""
    __tablename__ = "companies"
    id = Column(Integer, primary_key=True)
    name = Column(String(255), nullable=False)
    # ...

# web/backend/app/models/booking.py (shared)
class Booking(Base):
    """Модель записи в tenant схеме"""
    __tablename__ = "bookings"
    id = Column(Integer, primary_key=True)
    # ...
```

#### web/backend/app/schemas/

**Правила:**
- Схемы для публичных данных в `public_schemas.py` (если нужно)
- Схемы для tenant данных использовать существующие файлы
- Все схемы должны иметь валидацию

```python
# ✅ ПРАВИЛЬНО: Отдельная схема для регистрации
from pydantic import BaseModel, EmailStr, Field

class CompanyRegistration(BaseModel):
    """Схема для регистрации компании"""
    name: str = Field(..., min_length=3, max_length=255, description="Название автосервиса")
    email: EmailStr
    telegram_bot_token: str = Field(..., min_length=50, max_length=500, description="Токен бота")
    plan_id: int = Field(..., ge=1, description="ID тарифного плана")
```

#### web/backend/app/services/

**Правила:**
- Сервисы для public схемы в `company_service.py`, `subscription_service.py`, `payment_service.py`
- Сервисы для tenant схем использовать существующие файлы
- Сервис для работы с Юкассой: `yookassa_service.py`

```python
# ✅ ПРАВИЛЬНО: Отдельный сервис для компаний
# web/backend/app/services/company_service.py

async def get_company_by_id(session: AsyncSession, company_id: int):
    """Получить компанию по ID (public схема)"""
    pass

# ✅ ПРАВИЛЬНО: Сервис для работы с Юкассой
# web/backend/app/services/yookassa_service.py

class YooKassaService:
    """Сервис для интеграции с Юкассой"""
    async def create_payment(self, amount: Decimal, description: str):
        """Создать платеж через Юкассу"""
        pass
```

---

## 🗄 Мульти-тенантность

### Tenant сессии

**КРИТИЧЕСКИ:** Всегда используй `get_tenant_session(company_id)` для работы с данными компаний!

```python
# ✅ ПРАВИЛЬНО: Использование tenant-сессии
async def get_user_bookings(company_id: int, user_id: int):
    """Получить записи пользователя в конкретной компании"""
    async for session in get_tenant_session(company_id):
        result = await session.execute(
            select(Booking).where(Booking.user_id == user_id)
        )
        return list(result.scalars().all())

# ❌ НЕПРАВИЛЬНО: Использование обычной сессии
async def get_user_bookings(company_id: int, user_id: int):
    result = await session.execute(
        select(Booking).where(Booking.user_id == user_id)
    )
    return list(result.scalars().all())
```

### Middleware

**КРИТИЧЕСКИ:** В bot/main.py сохраняй контекст компании в диспетчере!

```python
# ✅ ПРАВИЛЬНО: Сохраняем контекст компании
async def run_bot_for_company(company: Company):
    """Запустить бота для конкретной компании"""
    bot = Bot(token=company.telegram_bot_token)
    dp = Dispatcher(storage=MemoryStorage())
    
    # Сохраняем контекст
    dp['company_id'] = company.id
    dp['company_name'] = company.name
    dp['schema_name'] = f'tenant_{company.id}'
    dp['can_create_bookings'] = company.can_create_bookings
    dp['subscription_status'] = company.subscription_status
    
    # Регистрируем роутеры
    # ...
    
    await dp.start_polling(bot, skip_updates=True)

# ❌ НЕПРАВИЛЬНО: Без контекста
async def run_bot():
    """Запустить все боты"""
    companies = await get_all_companies()
    
    for company in companies:
        bot = Bot(token=company.telegram_bot_token)
        dp = Dispatcher(storage=MemoryStorage())
        
        # Без контекста - не знаем для какой компании работает бот!
        await dp.start_polling(bot, skip_updates=True)
```

---

## 💾 База данных

### Транзакции

**КРИТИЧЕСКИ:** Всегда используй транзакции для критических операций!

```python
# ✅ ПРАВИЛЬНО: Использование транзакций
async def create_company(session: AsyncSession, name: str) -> Company:
    """Создать новую компанию"""
    async with session.begin():
        company = Company(name=name, is_active=True)
        session.add(company)
        
        # Создаем подписку
        subscription = await create_subscription(session, {
            "company_id": company.id,
            "plan_id": 3,  # Business
            "start_date": date.today(),
            "end_date": date.today() + timedelta(days=30)
        })
        
        session.add(subscription)
        await session.commit()
        return company

# ❌ НЕПРАВИЛЬНО: Без транзакций
async def create_company(session: AsyncSession, name: str) -> Company:
    company = Company(name=name, is_active=True)
    session.add(company)
    await session.commit()
    
    # Если создание подписки упадет, компания создана, а подписка - нет
    # Несогласованность данных!
```

### Миграции

**КРИТИЧЕСКИ:** Миграции должны быть атомарными!

```bash
# ✅ ПРАВИЛЬНО: Создание миграции с downgrade
alembic revision --autogenerate -m "Создать миграцию для X"

# ❌ НЕПРАВИЛЬНО: Без возможности отката
alembic revision --autogenerate -m "Создать миграцию для X"
# Если после применения миграции что-то пошло не так, нельзя вернуть состояние
```

---

## 🌐 Backend

### API endpoints

**Правила:**
- Все endpoints должны иметь docstrings на русском
- Публичные endpoints не требуют авторизации
- Tenant endpoints требуют `company_id` в запросе или middleware
- Все endpoints должны обрабатывать ошибки и возвращать понятные сообщения

```python
# ✅ ПРАВИЛЬНО: Публичный endpoint с docstring
@router.post("/public/companies/register", status_code=201)
async def register_company(request: CompanyRegistration, db: AsyncSession = Depends(get_db)):
    """
    Публичная регистрация новой компании.
    
    Проверяет валидность токена бота, создает платеж через Юкассу,
    создает компанию и подписку после оплаты.
    
    Returns:
        Ссылку на оплату через Юкассу
    """
    pass

# ❌ НЕПРАВИЛЬНО: Без docstring
@router.post("/public/companies/register", status_code=201)
async def register_company(request: CompanyRegistration, db: AsyncSession = Depends(get_db)):
    pass
```

### Аутентификация

**КРИТИЧЕСКИ:** JWT токены должны содержать `company_id` для tenant данных!

```python
# ✅ ПРАВИЛЬНО: JWT с company_id
def create_access_token(company_id: int, user_id: int) -> str:
    """
    Создать JWT токен с указанием компании.
    
    Args:
        company_id: ID компании для доступа к tenant схеме
        user_id: ID пользователя
        
    Returns:
        JWT токен
    """
    token_data = {
        "sub": user_id,
        "company_id": company_id,
        "exp": datetime.utcnow() + timedelta(days=1)
    }
    return encode_jwt(token_data)

# ❌ НЕПРАВИЛЬНО: Без company_id
def create_access_token(user_id: int) -> str:
    token_data = {
        "sub": user_id,
        "exp": datetime.utcnow() + timedelta(days=1)
    }
    return encode_jwt(token_data)
```

---

## 🎨 Frontend

### TypeScript типизация

**КРИТИЧЕСКИ:** Используй явные типы для всех данных!

```typescript
// ✅ ПРАВИЛЬНО: Явные типы
interface Booking {
    id: number;
    companyId: number;
    date: string;
    time: string;
    status: 'new' | 'confirmed' | 'completed' | 'cancelled';
    client: {
        id: number;
        fullName: string;
        phone: string;
    };
    service: {
        id: number;
        name: string;
        price: number;
        duration: number;
    };
}

// ❌ НЕПРАВИЛЬНО: Использование any
interface Booking {
    id: number;
    companyId?: number;
    date: any;
    time: any;
    status: any;
    client: any;
    service: any;
}
```

### React компоненты

**Правила:**
- Используй функциональные компоненты (Functional Components)
- Разделяй логику на хуки и эффекты
- Используй контекст для передачи данных

```typescript
// ✅ ПРАВИЛЬНО: Разделение логики
const BookingForm = ({ companyId, onSubmit }) => {
    const [formData, setFormData] = useState({ date: '', time: '', serviceId: 0 });
    
    const handleSubmit = async (e) => {
        e.preventDefault();
        
        // Валидация
        if (!formData.date || !formData.time || !formData.serviceId) {
            alert('Пожалуйста, заполните все поля');
            return;
        }
        
        // Отправка данных
        try {
            const booking = await createBooking(companyId, formData);
            onSubmit(booking);
        } catch (error) {
            console.error('Ошибка создания записи:', error);
            alert('Не удалось создать запись');
        }
    };
    
    return <form>...</form>
};

// ❌ НЕПРАВИЛЬНО: Вся логика в одном компоненте
const BookingForm = () => {
    const [formData, setFormData] = useState({ date: '', time: '', serviceId: 0 });
    
    const handleSubmit = async (e) => {
        e.preventDefault();
        
        // Без валидации - сразу отправляем
        const booking = await createBooking(companyId, formData);
        onSubmit(booking);
    };
    
    return <form>...</form>
};
```

---

## 🤖 Telegram Bot

### Мульти-боты

**КРИТИЧЕСКИ:** Каждый бот должен работать только со своей схемой!

```python
# ✅ ПРАВИЛЬНО: Использование tenant-сессии
async def run_bot_for_company(company: Company):
    """Запустить бота для конкретной компании"""
    async for session in get_tenant_session(company.id):
        # Все операции в этом боте будут с tenant схемой
        pass
    
    # Настройка бота
    bot = Bot(token=company.telegram_bot_token)
    dp = Dispatcher(storage=MemoryStorage())
    
    # Контекст бота
    dp['company_id'] = company.id
    dp['company_name'] = company.name
    dp['schema_name'] = f'tenant_{company.id}'
    dp['can_create_bookings'] = company.can_create_bookings
    dp['subscription_status'] = company.subscription_status
    
    # Регистрируем роутеры
    # ...
    
    await dp.start_polling(bot, skip_updates=True)

# ❌ НЕПРАВИЛЬНО: Один бот для всех компаний
async def run_bot():
    """Запустить все боты"""
    companies = await get_all_companies()
    
    for company in companies:
        bot = Bot(token=company.telegram_bot_token)
        dp = Dispatcher(storage=MemoryStorage())
        
        # Нет разделения по схемам - все боты в одной куче
        await dp.start_polling(bot, skip_updates=True)
```

### Обработчики

**Правила:**
- Все хендлеры должны извлекать `company_id` из контекста диспетчера
- Все хендлеры должны проверять подписку перед созданием записей
- Все сообщения должны быть на русском

```python
# ✅ ПРАВИЛЬНО: Извлекаем company_id из контекста
@router.message(F.text == "📅 Записаться")
async def start_booking(message: Message, state: FSMContext):
    """Начать процесс записи"""
    # Получаем company_id из контекста
    company_id = state.resolve_dp().get('company_id')
    can_create_bookings = state.resolve_dp().get('can_create_bookings', True)
    
    if not can_create_bookings:
        await message.answer(
            "❌ Запись отключена.\n\n"
            "Ваша подписка истекла.\n"
            "Пожалуйста, продлите подписку для создания записей."
        )
        return
    
    # Продолжаем процесс создания записи
    # ...

# ❌ НЕПРАВИЛЬНО: Без проверки подписки
@router.message(F.text == "📅 Записаться")
async def start_booking(message: Message, state: FSMContext):
    """Начать процесс записи"""
    # Нет проверки подписки - позволяем создавать записи даже при неоплате
    # ...

    await message.answer("Выберите услугу:")
```

---

## ⚙ Celery Tasks

### Напоминания

**КРИТИЧЕСКИ:** Все напоминания об оплате должны быть на русском!

```python
# ✅ ПРАВИЛЬНО: Напоминание на русском
@shared_task
def send_reminder_7_days_before():
    """Отправить напоминание за 7 дней до окончания подписки"""
    async for session in get_session_with_schema("public"):
        companies = await session.execute(
            select(Company).where(
                Company.subscription_end_date == date.today() + timedelta(days=7),
                Company.is_active == True
            )
        ).scalars().all()
        
        for company in companies:
            await send_telegram_notification(
                company.admin_telegram_id,
                f"⚠️ Внимание!\n"
                f"Подписка компании '{company.name}' истекает через 7 дней.\n"
                f"Пожалуйста, продлите подписку."
            )

# ❌ НЕПРАВИЛЬНО: Напоминание на английском
@shared_task
def send_reminder_7_days_before():
    """Отправить напоминания"""
    async for session in get_session_with_schema("public"):
        # ...
```

---

## 🔒 Безопасность

### Валидация данных

**КРИТИЧЕСКИ:** Всегда валидируй данные от пользователей!

```python
# ✅ ПРАВИЛЬНО: Валидация во всех API
from pydantic import BaseModel, EmailStr, Field, validator

class CompanyRegistration(BaseModel):
    """Схема для регистрации компании с валидацией"""
    name: str = Field(..., min_length=3, max_length=255, description="Название автосервиса")
    email: EmailStr
    phone: str = Field(..., max_length=20, pattern=r'^\+?7\d{10}$', description="Телефон")
    telegram_bot_token: str = Field(..., min_length=50, max_length=500, description="Токен бота")
    
    @validator('telegram_bot_token')
    def validate_bot_token(cls, v):
        """Валидировать токен через Telegram API"""
        return True  # Здесь будет проверка

# ❌ НЕПРАВИЛЬНО: Без валидации
class CompanyRegistration(BaseModel):
    """Схема для регистрации компании"""
    name: str
    email: str
    phone: str
    telegram_bot_token: str
    plan_id: int
```

### Избегание SQL инъекций

**КРИТИЧЕСКИ:** Всегда используй параметризованные запросы!

```python
# ✅ ПРАВИЛЬНО: Параметризованные запросы
async def get_user_by_id(user_id: int):
    """Получить пользователя по ID с защитой от SQL инъекций"""
    async for session in get_session():
        result = await session.execute(
            select(User).where(User.id == bindparam('user_id', user_id))
        )
        return result.scalar_one_or_none()

# ❌ НЕПРАВИЛЬНО: Форматирование строк (SQL инъекция)
async def get_user_by_id(user_id: int):
    """Получить пользователя по ID"""
    query = f"SELECT * FROM users WHERE id = {user_id}"
    result = await session.execute(text(query))
    return result.scalar_one_or_none()
```

### Защита секретов

**КРИТИЧЕСКИ:** Никогда не логируй токены и пароли!

```bash
# ✅ ПРАВИЛЬНО: Логирование без секретов
logging.info(f"Компания создана: {name}")
logging.info(f"Платеж создан: {payment_id}")

# ❌ НЕПРАВИЛЬНО: Логирование секретов
logging.info(f"Компания создана: {name}")
logging.info(f"Токен бота: {bot_token}")
logging.info(f"Платеж: {yookassa_payment_id}")
```

---

## 📝 Git и коммиты

### Частые коммиты

**Правила:**
- Делай частые коммиты с понятными сообщениями
- Коммиты только после завершения логической части
- Сообщения на русском языке

```bash
# ✅ ПРАВИЛЬНО: Частые коммиты с понятными сообщениями
git add .
git commit -m "feat(backend): Добавлен сервис работы с Юкассой

- Добавлен YooKassaService для создания платежей
- Добавлена валидация подписи webhook от Юкассы
- Добавлены тесты для интеграции"

git add .
git commit -m "fix(bot): Исправлена проверка подписки

- Исправлена ошибка, когда company.can_create_bookings был None
- Добавлена проверка на None перед блокировкой
- Исправлены сообщения об истекшей подписке на русском"
```

### Сообщения коммитов

**Типы сообщений:**
- `feat:` - новая функция
- `fix:` - исправление ошибки
- `refactor:` - рефакторинг кода
- `docs:` - документация
- `style:` - исправление стиля
- `test:` - добавление тестов

```bash
# ✅ ПРАВИЛЬНО: Структурированные сообщения коммитов
git add .
git commit -m "feat(backend): Добавлен публичный API регистрации

# Создан POST /api/public/companies/register
# Валидация токена бота через Telegram API
# Интеграция с Юкассой для создания платежей
# Автоматическое создание подписки после оплаты
# Возвращает ссылку на оплату через Юкассу

git add .
git commit -m "fix(bot): Исправлена проверка подписки при создании записи

# Исправлена ошибка, когда company.can_create_bookings был None
# Добавлена проверка на None перед блокировкой
# Исправлены сообщения об истекшей подписке на русском"

git add .
git commit -m "refactor(database): Реализована мульти-тенантность

# Добавлен TenantService для управления схемами
# Обновлены все CRUD функции для работы с company_id
# Создан Middleware для проверки подписки и определения компании
# Применены миграции для создания tenant схем"

git add .
git commit -m "docs: Обновлена документация проекта

# Обновлен README.md с описанием мульти-тенантной архитектуры
# Созданы 14 md файлов с этапами реализации
# Добавлены инструкции по настройке GitHub
# Обновлена документация для разработчиков"
```

---

## 🚀 Рекомендации по порядку работы

### 1. Изучи проект
- Прочитай `md/00_PROJECT_OVERVIEW.md`
- Изучи существующую архитектуру
- Пойми текущие ограничения

### 2. Начни с документации
- Прочитай файлы этапов в папке `md/`
- Следуй порядку от этапа 1 до этапа 13
- Отмечай выполненное в чек-листах

### 3. Тестируй часто
- Тестируй каждую функцию перед переходом к следующей
- Используй тестовую БД для экспериментов
- Не накапливай тесты на конец

### 4. Коммить часто
- Делай частые коммиты с понятными сообщениями
- Не коммитай сломанный код
- Коммиты только после завершения логической части

### 5. Делай бэкапы
- Делай бэкапы базы данных перед критическими изменениями
- Создавай бэкапы кода перед большими рефакторингами

### 6. Следуй правилам кода
- Все комментарии на русском
- Все docstrings на русском
- Type hints во всем Python коде
- Явные типы в TypeScript

### 7. Проверяй изоляцию
- Всегда проверяй, что код работает с правильной схемой
- Тестируй, что данные одной компании недоступны другой

### 8. Коммуникативно
- Если возникают вопросы - задавай их сразу
- Не бойся признать, что нужно перепроектировать

---

## 📚 Поддержка и ресурсы

### Документация

- `md/00_PROJECT_OVERVIEW.md` - полный обзор проекта
- `md/01_DATABASE_MIGRATION.md` - инструкции по миграции
- `md/02_BACKEND_MODELS.md` - модели и схемы
- `md/03_BACKEND_MULTI_TENANT.md` - мульти-тенантность
- `md/04_BACKEND_PUBLIC_API.md` - публичные API
- `md/05_BACKEND_SUPER_ADMIN.md` - API супер-админа
- `md/06_CELERY_TASKS.md` - Celery задачи
- `md/07_BOT_MULTI_TENANT.md` - мульти-боты
- `md/08_FRONTEND_REGISTRATION.md` - Frontend регистрация
- `md/09_FRONTEND_SUPER_ADMIN.md` - Админ-панель супера
- `md/10_FRONTEND_UPDATE.md` - Обновление Frontend
- `md/11_TESTING.md` - Тестирование
- `md/12_DEPLOYMENT.md` - Деплой
- `md/13_DOCUMENTATION.md` - Документация

---

## 🎯 Цель проекта

Создать мульти-тенантную SaaS платформу для управления автосервисами с:
- Изоляцией данных между клиентами
- Системой подписок и платежей через Юкассу
- Автоматическими напоминаниями об оплате
- Мульти-бот системой для разных компаний
- Админ-панелью для супер-админа
- Публичной страницей регистрации для новых клиентов

**Удачной разработки!** 🚀
