# 🔧 Исправления: Специализации и данные Клиентов/Пользователей

## ✅ Исправления

### 1. Специализации мастеров
**Статус:** ✅ Уже обновлены в коде

Специализации в `web/frontend/src/pages/Masters.tsx` (строки 488-498) уже содержат правильные значения:
- Парикмахер
- Визажист
- Ногтевой мастер
- Косметолог
- Барбер
- Колорист
- Стилист
- Массажист
- Лэшмейкер
- Бровист

**Если на странице все еще старые специализации:**
1. Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)
2. Перезагрузите страницу
3. Проверьте, что фронтенд пересобран: `docker compose restart web-frontend`

### 2. Нет данных в Клиентах и Пользователях
**Проблема:** API не находил таблицы в tenant схеме

**Исправление:**
- Добавлен `SET LOCAL search_path` для текущей транзакции в `users_tenant.py` и `clients_tenant.py`
- Это гарантирует, что SQLAlchemy будет искать таблицы в правильной схеме

**Измененные файлы:**
- `web/backend/app/api/users_tenant.py`
- `web/backend/app/api/clients_tenant.py`

## 🧪 Проверка

### Проверить данные в БД:
```bash
# Клиенты
docker compose exec postgres psql -U barber_user -d barber_db -c "SELECT COUNT(*) FROM tenant_8.clients;"

# Пользователи
docker compose exec postgres psql -U barber_user -d barber_db -c "SELECT COUNT(*) FROM tenant_8.users;"
```

### Перезапустить сервисы:
```bash
# Backend
docker compose restart web

# Frontend (если нужно)
docker compose restart web-frontend
```

## 📝 Примечания

1. **Специализации:** Код уже обновлен. Если видите старые значения - очистите кэш браузера
2. **Данные:** После перезапуска backend данные должны появиться в интерфейсе
3. **Проверка:** Убедитесь, что вы вошли как пользователь компании (не супер-админ), чтобы видеть данные tenant схемы
