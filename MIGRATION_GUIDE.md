# Руководство по миграции базы данных

## 📋 Обзор

Это руководство описывает процесс применения миграций к базе данных для AutoService SaaS платформы.

## 🔄 Применение миграций

### Вариант 1: Внутри Docker контейнера (рекомендуется)

```bash
# 1. Запустите контейнер backend
docker-compose up -d web

# 2. Подключитесь к контейнеру
docker exec -it autoservice_web bash

# 3. Примените миграции
cd /app
python scripts/migrate.py

# 4. Заполните БД начальными данными
python scripts/seed.py

# 5. Выйдите из контейнера
exit
```

### Вариант 2: Локально (без Docker)

```bash
# 1. Убедитесь, что все зависимости установлены
cd web/backend
pip install -r requirements.txt

# 2. Примените миграции
python scripts/migrate.py

# 3. Заполните БД начальными данными
python scripts/seed.py
```

### Вариант 3: Через Alembic CLI

```bash
cd web/backend

# Применить все миграции
alembic upgrade head

# Посмотреть текущую версию
alembic current

# Посмотреть историю миграций
alembic history

# Откатить на одну миграцию назад
alembic downgrade -1
```

## 🌱 Начальные данные (Seed Data)

Скрипт `seed.py` создает:

### Тарифные планы (3 шт.)

| План | Цена/месяц | Цена/год | Записи/месяц | Пользователи | Мастера | Посты | Акции |
|------|------------|----------|--------------|--------------|---------|-------|-------|
| Starter | 2 990 ₽ | 29 900 ₽ | 50 | 5 | 3 | 5 | 2 |
| Pro | 5 990 ₽ | 59 900 ₽ | 200 | 20 | 10 | 20 | 10 |
| Business | 11 990 ₽ | 119 900 ₽ | 1000 | 100 | 50 | 100 | 50 |

### Супер-администратор

- **Username**: `admin` (по умолчанию, можно изменить в .env)
- **Email**: `admin@autoservice.com` (по умолчанию, можно изменить в .env)
- **Password**: `admin123` (по умолчанию, можно изменить в .env)

⚠️ **Важно:** После первого входа обязательно смените пароль!

## ⚙️ Настройка переменных окружения

Добавьте следующие переменные в `.env` файл:

```bash
# Супер-администратор
SUPER_ADMIN_USERNAME=admin
SUPER_ADMIN_EMAIL=admin@autoservice.com
SUPER_ADMIN_PASSWORD=admin123

# База данных
DB_HOST=postgres
DB_PORT=5432
DB_NAME=autoservice_db
DB_USER=autoservice_user
DB_PASSWORD=your_secure_password_here

# Yookassa
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key

# Webhook
WEBHOOK_RETURN_URL=https://yourdomain.com/register/success
WEBHOOK_URL=https://yourdomain.com/api/public/webhooks/yookassa

# Email (для SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
EMAIL_FROM=noreply@autoservice.com
```

## 🧪 Проверка миграций

### Проверка созданных таблиц

```bash
# Подключитесь к PostgreSQL
docker exec -it autoservice_postgres psql -U autoservice_user -d autoservice_db

# Посмотреть список схем
\dn

# Посмотреть таблицы в public схеме
\dt public.*

# Посмотреть тарифные планы
SELECT * FROM public.plans;

# Посмотреть супер-администраторов
SELECT username, email, is_active FROM public.super_admins;

# Выйти из PostgreSQL
\q
```

### Проверка через API

```bash
# Получить список тарифных планов
curl http://localhost:8000/api/public/plans

# Проверить статус API
curl http://localhost:8000/api/health
```

## 🔄 Откат миграций

⚠️ **Осторожно:** Откат миграций удалит данные!

```bash
# Откатить на одну миграцию назад
alembic downgrade -1

# Откатить к базовой версии
alembic downgrade base
```

## 📝 Создание новых миграций

После изменения моделей:

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить новую миграцию
alembic upgrade head
```

## 🐛 Устранение проблем

### Ошибка: "relation already exists"

Это может произойти, если миграции применялись частично.

**Решение:**
```bash
# Проверить текущую версию
alembic current

# Сбросить и применить миграции заново
alembic downgrade base
alembic upgrade head
```

### Ошибка: "database is locked"

Это может произойти, если есть активные подключения к БД.

**Решение:**
```bash
# Остановите все контейнеры, использующие БД
docker-compose down

# Запустите только postgres
docker-compose up -d postgres

# Примените миграции
docker exec -it autoservice_web python scripts/migrate.py

# Запустите все контейнеры
docker-compose up -d
```

### Ошибка: "No such module 'app'"

Проверьте, что находитесь в правильной директории и Python видит модули.

**Решение:**
```bash
cd web/backend
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
python scripts/migrate.py
```

## 📚 Полезные команды

```bash
# Посмотреть логи контейнера web
docker-compose logs -f web

# Перезапустить контейнер web
docker-compose restart web

# Посмотреть список миграций
ls -la alembic/versions/

# Посмотреть содержимое миграции
cat alembic/versions/001_create_multi_tenant_tables.py

# Проверить статус контейнеров
docker-compose ps

# Очистить все контейнеры и volumes (осторожно!)
docker-compose down -v
```

## ✅ Проверка успешности

После применения миграций убедитесь:

1. ✅ Таблицы созданы в `public` схеме (`plans`, `companies`, `subscriptions`, `payments`, `super_admins`)
2. ✅ Индексы созданы
3. ✅ Тарифные планы загружены (3 плана)
4. ✅ Супер-администратор создан
5. ✅ API отвечает на запросы

## 🎯 Следующие шаги

После успешного применения миграций:

1. Заполните БД начальными данными (`python scripts/seed.py`)
2. Проверьте работу API (`http://localhost:8000/api/health`)
3. Протестируйте страницу регистрации (`http://localhost:3000/register`)
4. Запустите боты через BotManager
5. Проведите полное тестирование системы

---

**Дополнительная информация:**
- [Alembic Documentation](https://alembic.sqlalchemy.org/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

