# 🧪 Инструкция по тестированию Этапа 10

## ✅ Что готово к тестированию

### Frontend
- ✅ Обновлен `auth.ts` с поддержкой `company_id` и `SubscriptionInfo`
- ✅ Обновлен `useAuth.tsx` с информацией о подписке
- ✅ Обновлен `Sidebar.tsx` с отображением статуса подписки
- ✅ Создан компонент `SubscriptionBarrier` для блокировки при истекшей подписке
- ✅ Обновлен `App.tsx` с защитой маршрутов

### Backend
- ✅ Создан endpoint `/api/subscription` для получения информации о подписке
- ✅ Создан endpoint `/api/subscription/check` для быстрой проверки статуса
- ✅ Router добавлен в `main.py`

## ⚠️ Важные замечания

### 1. Требования к базе данных

Endpoint `/api/subscription` требует:
- ✅ Public схему с таблицами:
  - `public.companies`
  - `public.subscriptions`
  - `public.plans`
- ✅ Связь между User и Company через `admin_telegram_id` или `telegram_admin_ids`

### 2. Как работает endpoint

Endpoint ищет компанию для пользователя следующим образом:
1. Получает `telegram_id` из текущего пользователя
2. Ищет компанию в `public.companies` где:
   - `admin_telegram_id = telegram_id` ИЛИ
   - `telegram_id` в массиве `telegram_admin_ids`
3. Получает последнюю подписку для найденной компании

### 3. Возможные проблемы

**Проблема 1: Компания не найдена**
- **Симптом:** 404 ошибка "Company not found for user"
- **Причина:** Пользователь не связан с компанией
- **Решение:** 
  - Создать компанию через `/api/public/companies/register`
  - Или связать пользователя с существующей компанией

**Проблема 2: Подписка не найдена**
- **Симптом:** 404 ошибка "Subscription not found for company"
- **Причина:** У компании нет подписки
- **Решение:** 
  - Создать подписку через админ-панель супер-админа
  - Или через webhook от Юкассы после оплаты

**Проблема 3: Public схема не создана**
- **Симптом:** Ошибка SQL "relation does not exist"
- **Причина:** Миграции не применены
- **Решение:** Применить миграции для public схемы

## 🚀 Как запустить и протестировать

### Шаг 1: Запустить сервисы

```bash
# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# Проверить логи backend
docker-compose logs -f web
```

### Шаг 2: Проверить базу данных

```bash
# Подключиться к БД
docker-compose exec postgres psql -U autoservice_user -d autoservice_db

# Проверить наличие public схемы
\dn

# Проверить наличие таблиц
\dt public.*

# Проверить наличие компаний
SELECT id, name, admin_telegram_id FROM public.companies;

# Проверить наличие подписок
SELECT id, company_id, status, end_date FROM public.subscriptions;
```

### Шаг 3: Проверить API

```bash
# Проверить health endpoint
curl http://localhost:8000/api/health

# Проверить subscription endpoint (требует авторизации)
curl -X GET http://localhost:8000/api/subscription \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Шаг 4: Протестировать Frontend

1. **Открыть браузер:**
   ```
   http://localhost:3000
   ```

2. **Войти в систему:**
   - Используйте существующие учетные данные
   - Или создайте нового пользователя через Telegram бота

3. **Проверить сайдбар:**
   - Должна отображаться информация о подписке
   - Если подписка истекла, должно быть предупреждение

4. **Проверить блокировку:**
   - Перейти на `/bookings` или `/calendar`
   - Если подписка неактивна, должен появиться `SubscriptionBarrier`

## 🐛 Отладка

### Проверить логи backend

```bash
docker-compose logs -f web
```

### Проверить логи frontend

```bash
docker-compose logs -f web-frontend
```

### Проверить консоль браузера

Откройте DevTools (F12) и проверьте:
- Ошибки в Console
- Запросы в Network tab
- Особенно запросы к `/api/subscription`

### Типичные ошибки

**Ошибка 1: "Company not found for user"**
```
Решение: Создать компанию или связать пользователя с компанией
```

**Ошибка 2: "Subscription not found for company"**
```
Решение: Создать подписку для компании
```

**Ошибка 3: "relation does not exist"**
```
Решение: Применить миграции для public схемы
```

## 📝 Чек-лист тестирования

- [ ] Backend запущен и отвечает на `/api/health`
- [ ] Frontend запущен и доступен на `http://localhost:3000`
- [ ] Public схема создана в БД
- [ ] Таблицы `companies`, `subscriptions`, `plans` существуют
- [ ] Есть хотя бы одна компания в БД
- [ ] Пользователь связан с компанией (через `admin_telegram_id`)
- [ ] У компании есть подписка
- [ ] Можно войти в систему через `/login`
- [ ] В сайдбаре отображается информация о подписке
- [ ] При истекшей подписке блокируются страницы `/bookings` и `/calendar`
- [ ] Компонент `SubscriptionBarrier` корректно отображается

## 🎯 Следующие шаги

После успешного тестирования:
1. Исправить найденные ошибки
2. Добавить обработку edge cases
3. Улучшить UX для случаев отсутствия подписки
4. Добавить автоматическое обновление информации о подписке

---

**Дата создания:** 06.01.2026  
**Версия:** 1.0

